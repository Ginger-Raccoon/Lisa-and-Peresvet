import s from './style.module.css'

const Footer = () => {
    return (
        <div className={s.footer}>
            <a className={s.footer__link} href="#header"><p>В Начало</p></a>
        </div>
    );
};

export default  Footer;
